{
  "name": "mobile-firebase-rules-tests",
  "version": "1.0.0",
  "description": "Firestore security rules unit tests for Rakcha mobile application",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npx --yes firebase-tools emulators:exec --project demo-rakcha --only firestore \"vitest run\"",
    "test:local": "vitest run"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "vitest": "^2.1.5"
  }
}
